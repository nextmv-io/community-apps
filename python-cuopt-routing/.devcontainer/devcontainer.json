{
  "image": "ghcr.io/nextmv-io/runtime/cuopt",
  "runArgs": ["--runtime=nvidia"],
  "postCreateCommand": "pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
