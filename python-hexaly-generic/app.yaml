# This manifest holds the information the app needs to run on the Nextmv Cloud.
type: python
runtime: ghcr.io/nextmv-io/runtime/hexaly:latest
python:
  # All listed packages will get bundled with the app.
  pip-requirements: requirements.txt

# List all files/directories that should be included in the app. Globbing
# (e.g.: configs/*.json) is supported.
files:
  - main.py

configuration:
  # Specify the format the app reads and writes.
  content:
    format: "multi-file" # Read and write files from disk.
    multi-file:
      input:
        path: "." # Read files from the current working directory.
      output:
        solutions: "." # Write solution files to the current working directory.
