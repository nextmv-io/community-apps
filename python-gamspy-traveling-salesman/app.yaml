# This manifest holds the information the app needs to run on the Nextmv Cloud.
type: python
runtime: ghcr.io/nextmv-io/runtime/python:3.11
python:
  # All listed packages will get bundled with the app.
  pip-requirements: requirements.txt
# List all files/directories that should be included in the app. Globbing
# (e.g.: configs/*.json) is supported.
files:
  - main.py
  - tsp.py

# Application configurations.
configuration:
  # Define the content format of the app, one of: json, multi-file.
  content:
    format: json
  # Define the options (parameters) of the app.
  options:
    strict: false
    items:
      - name: maxnodes
        option_type: int
        default: 5
        description: Maximum number of nodes to solve the model with
        required: false
        additional_attributes:
          min: 1
          max: 100
          step: 1
        ui:
          control_type: slider
          display_name: Maximum Nodes